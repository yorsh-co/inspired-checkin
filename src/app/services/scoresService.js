const ScoresService = (() => {
  /**
   *
   * @param {string} userId
   * @returns {{ email: string, alternatives: [string], name: string, scores: [{ formTitle: string, formPoints: string, score: number }]}}
   */
  const getByUser = (userId) => {
   
  };

  return {
    getByUser,
  };
})();
